게임 목적	
	JS언어에 익숙해지기
	객체지향 구현하기
게임 구조
	말하는대로


canvas
	캐릭터 구조화 -> 캡슐화

캡슐화 -> this문제 발생
	Function.prototype.call()
		함수 안의 객체는 함수를 호출한 놈
	.apply	
	.bind

윈도우 이벤트 수신기능

플랫폼과 API(이용하고 싶지만 이용할 방법)
윈도우
문서에서 보여지는 모든 객체
이벤트 버블링
	- frame윈도우: top윈도우(frame, 다른 영역과 구분)
	- 임베디드 윈도우, 자식 윈도우
	